<?xml version="1.0" encoding="utf-8"?>
<manifest
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.assess15.code">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:hardwareAccelerated="false"
            tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".paint.PaintActivity"/>
        <activity android:name=".paint.xfermode.XfermodeActivity"/>
        <activity android:name=".paint.gradient.GradientActivity"/>
        <activity android:name=".paint.colorFilter.ColorFilterActivity"/>
        <activity android:name=".canvas.CanvasActivity"/>
        <activity android:name=".path.PathActivity"/>
        <activity android:name=".pathMeasure.PathMeasureActivity"/>
    </application>

</manifest>